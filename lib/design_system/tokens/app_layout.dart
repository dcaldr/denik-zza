class AppLayout {
  AppLayout._();

  /// Flex value for main input fields in a row (e.g. title)
  static const int inputFlex = 2;

  /// Flex value for actions/buttons section in a row
  static const int actionsFlex = 1;

  /// Default spacing between input and actions
  static const double actionSpacing = 16.0;
}
