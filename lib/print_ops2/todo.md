# todo 
[] multipage support 
    - maybe try counting pages first without the new record(s), then with them, if different -> needs new page(s) (and we know how many)
    - but that doesn't solve situation when new record(s) fit on existing page, but push some existing record(s) to new page vs when we are adding only new pages
